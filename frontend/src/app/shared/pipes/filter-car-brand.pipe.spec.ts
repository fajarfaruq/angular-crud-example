import { FilterCarBrandPipe } from './filter-car-brand.pipe';

describe('FilterCarBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
